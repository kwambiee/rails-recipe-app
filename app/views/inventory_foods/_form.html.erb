<%= form_with model: inventory_food, url: inventory_inventory_foods_path, method: :post, local: true, scope: :inventory_food do |form| %>
    <% if inventory_food.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(inventory_food.errors.count, "error") %> prohibited this inventory_food from being saved:</h2>

      <ul>
        <% inventory_food.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :quantity, style: "display: block" %>
    <%= form.number_field :quantity %>
  </div>

  <div>
    <%= form.label :food, style: "display: block" %>
    <%= form.select(:food_id, @food_list.map {|food| [food.name, food.id]}, class: 'select_input_form' ) %>

  </div>


  <div>
    <%= form.submit %>
  </div>
<% end %>
